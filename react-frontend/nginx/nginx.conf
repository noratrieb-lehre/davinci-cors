server {
    listen ${NGINX_PORT};

    location ^~ /api/ {
        proxy_pass ${BACKEND_URL};
    }

    location / {
        root /usr/share/nginx/html;
        try_files $uri /$uri /index.html;
    }
}

